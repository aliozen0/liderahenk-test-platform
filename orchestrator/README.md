# Orchestrator

This directory contains the legacy/simple flow runner and its CLI entry points.

- `main.py` and `cli.py` operate on `orchestrator/legacy_scenarios/`.
- These scenarios remain useful for direct workflow runs and compatibility
  tests.
- This directory is still active, but it is not the same thing as the newer
  scenario-pack layer.

If you are working on runtime operational acceptance or release-gate scenario
packs, use `/home/huma/liderahenk-test/platform/scenarios/`.
