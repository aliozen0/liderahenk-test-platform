# Contract Tests

This directory contains pytest-based contract tests.

- Use this path for test code that validates LDAP, REST, or XMPP behavior.
- `make test-contract` and the contract-specific make targets execute tests from here.
- This is not the canonical home of platform contract definitions.

If you need runtime or declarative contract files, use
`/home/huma/liderahenk-test/platform/contracts/`.
